<?xml version="1.0" encoding="utf-8" ?>
<!--For all properties see: https://aka.ms/android-menu-resource-->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

	<group>
		<item
			android:id="@+id/profileNav"
			android:title="Profile"
			android:icon="@drawable/profile"/>

		<item
			android:id="@+id/ownActivityNav"
			android:title="Add your own activity"
			android:icon="@drawable/add"/>

		<item
			android:id="@+id/inviteFriendNav"
			android:title="Invite a friend"
			android:icon="@drawable/invite"/>

		<item
			android:id="@+id/logoutNav"
			android:title="Logout"
			android:icon="@drawable/logout"/>
	</group>

</menu>

<!-- Code to implement into Activity:

Android.Widget.SearchView searchView;
public override bool OnCreateOptionsMenu(IMenu menu)
{
  this.MenuInflater.Inflate(Resource.Menu.navmenu, menu);

  var searchItem = menu.FindItem(Resource.Id.action_search);

  searchView = searchItem.ActionProvider.JavaCast<Android.Widget.SearchView>();

  searchView.QueryTextSubmit += (sender, args) =>
  {
    Toast.MakeText(this, "You searched: " + args.Query, ToastLength.Short).Show();

  };


  return base.OnCreateOptionsMenu(menu);
}

-->
